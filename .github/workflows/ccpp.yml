name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: [ubuntu-latest]
    
    
    steps:
    - uses: actions/checkout@v2
    - name: cmake
      run: cmake
    - name: cmake check
      run: cmake check
    - name: cmake distcheck
      run: cmake distcheck
